services:
  barbershop:
    build: .
    ports:
      - "3000:3000"
    environment:
      - CMS_PASSWORD=BlackBlade2026!
      - CMS_JWT_SECRET=blackblade-jwt-secret-2026-super-secure-key
    volumes:
      - cms_data:/app/data
    restart: unless-stopped

volumes:
  cms_data:
